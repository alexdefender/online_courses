import React from 'react';
import Pagination from '@mui/material/Pagination';

const PaginationComponent = (props) => <Pagination color="primary" size="large" {...props} />;

export default PaginationComponent;
