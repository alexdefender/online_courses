import React from 'react';
import ExpandMoreIcon from '@mui/icons-material/ExpandMore';

const ExpandMoreIconComponent = (props) => <ExpandMoreIcon {...props} />;

export default ExpandMoreIconComponent;
