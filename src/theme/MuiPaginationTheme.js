import { spacing } from './handlers';

const MuiPaginationTheme = {
  styleOverrides: {
    root: {
      marginTop: spacing(4),
    },
    ul: {
      justifyContent: 'center',
    },
  },
};

export default MuiPaginationTheme;
